import { TouchableOpacity } from "react-native-gesture-handler";
import { styles } from "./AddImages.styles";
import { FlatListProps, Image, ListRenderItem, Text } from "react-native";

export const renderItem = () => (
  
);
