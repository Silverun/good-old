export const AuthRoutes = {
  login: "Login",
  register: "Register",
} as const;

export const MainRoutes = {
  lots: "Lots",
  profile: "Profile",
  myLots: "MyLots",
} as const;

export const RootRoutes = {
  auth: "Auth",
  main: "Main",
} as const;
