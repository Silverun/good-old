import { View } from "react-native";
import { TextCustom } from "../../../components/common";

const LotsScreen = () => {
  return (
    <View>
      <TextCustom>LotsScreen</TextCustom>
    </View>
  );
};
export default LotsScreen;
